# references/

Put supporting detail here — full tables, policy excerpts, examples, checklists — and
link to it from `SKILL.md`. This keeps `SKILL.md` focused (always-loaded) while detail is
pulled in on demand.

Delete this folder if your skill doesn't need it.
